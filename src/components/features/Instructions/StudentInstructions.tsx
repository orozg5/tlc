import IUserProps from '@/interfaces/IUserProps'
import React from 'react'

export default function StudentInstructions({ userData }: IUserProps) {
  return (
    <div>StudentInstructions</div>
  )
}
