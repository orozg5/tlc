export default interface IStudentsMaterial {
  id: string;
  material_id: string;
  student_id: string;
  expiry_date: string;
  instructor_id: string;
  is_public: boolean;
  subject_id: string;
  file_url: string;
  file_name: string;
  path: string;
}
